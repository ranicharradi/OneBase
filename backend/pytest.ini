[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = --cov=app --cov-report=term-missing --cov-fail-under=35
markers =
    slow: marks tests as slow (ML model loading, embedding, training)
