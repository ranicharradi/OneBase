---
phase: 1.1-design-polish
plan: 03
type: execute
wave: 2
depends_on: ["1.1-01"]
files_modified:
  - frontend/src/pages/Upload.tsx
  - frontend/src/components/DropZone.tsx
  - frontend/src/components/ColumnMapper.tsx
  - frontend/src/components/ProgressTracker.tsx
  - frontend/src/components/ReUploadDialog.tsx
  - frontend/src/components/BatchHistory.tsx
autonomous: true
requirements: [OPS-06]

must_haves:
  truths:
    - "Upload page uses the design system from Plan 01 — consistent visual language"
    - "DropZone has atmospheric drag-over state with animation — not just a border color change"
    - "ProgressTracker pipeline stages feel alive — animated transitions between stages, not static icons"
    - "ColumnMapper wizard steps have visual polish — progress indicator, transitions between steps"
    - "BatchHistory table matches the Users page table design language"
  artifacts:
    - path: "frontend/src/pages/Upload.tsx"
      provides: "Redesigned Upload orchestrator page"
      min_lines: 250
    - path: "frontend/src/components/DropZone.tsx"
      provides: "Atmospheric drag-and-drop zone"
      min_lines: 150
    - path: "frontend/src/components/ColumnMapper.tsx"
      provides: "Polished column mapping wizard"
      min_lines: 200
    - path: "frontend/src/components/ProgressTracker.tsx"
      provides: "Animated pipeline progress visualization"
      min_lines: 170
    - path: "frontend/src/components/ReUploadDialog.tsx"
      provides: "Styled re-upload confirmation dialog"
      min_lines: 70
    - path: "frontend/src/components/BatchHistory.tsx"
      provides: "Styled batch history table"
      min_lines: 120
  key_links:
    - from: "frontend/src/app.css"
      to: "frontend/src/pages/Upload.tsx"
      via: "Design tokens and animations from Plan 01"
      pattern: "surface-|accent-|animate-"
    - from: "frontend/src/pages/Upload.tsx"
      to: "frontend/src/components/DropZone.tsx"
      via: "Upload page imports and renders DropZone component"
      pattern: "DropZone"
    - from: "frontend/src/pages/Upload.tsx"
      to: "frontend/src/components/ProgressTracker.tsx"
      via: "Upload page imports and renders ProgressTracker"
      pattern: "ProgressTracker"
---

<objective>
Apply the design system to the entire upload experience — the Upload page orchestrator and all five upload-related components. This is the most interaction-rich part of the UI: drag-and-drop, multi-step wizard, real-time progress, confirmation dialogs, and history tables.

Purpose: The upload flow is the core user workflow. Drag-and-drop, column mapping, and progress tracking need to feel responsive and polished. These components have the most motion/interaction opportunities.
Output: Redesigned Upload.tsx and all 5 upload components with distinctive styling matching the Plan 01 design system.
</objective>

<execution_context>
@/home/rani/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rani/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.agents/skills/frontend-design/SKILL.md
@.planning/phases/1.1-design-polish/1.1-01-SUMMARY.md

Read the updated `frontend/src/app.css` to understand the design system from Plan 01.

**Current state of files being modified:**
- `frontend/src/pages/Upload.tsx` — 303 lines. 4-state machine orchestrator (SELECT_SOURCE → UPLOAD_FILE → MAP_COLUMNS → PROCESSING) rendering different components per state.
- `frontend/src/components/DropZone.tsx` — 181 lines. Drag-and-drop file upload with CSV filtering, drag-over visual feedback, Browse button.
- `frontend/src/components/ColumnMapper.tsx` — 241 lines. 2-step wizard: Step 1 name/describe source → Step 2 map canonical fields to CSV headers via dropdowns.
- `frontend/src/components/ProgressTracker.tsx` — 206 lines. 4-stage pipeline visualization (Parsing → Normalizing → Embedding → Match Enqueued) with animated spinner on active stage.
- `frontend/src/components/ReUploadDialog.tsx` — 92 lines. Modal warning dialog for re-upload with impact counts.
- `frontend/src/components/BatchHistory.tsx` — 147 lines. Table of upload history with status color coding.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Upload page and DropZone</name>
  <files>frontend/src/pages/Upload.tsx, frontend/src/components/DropZone.tsx</files>
  <action>
Read both files and the Plan 01 SUMMARY + updated `frontend/src/app.css`. Load the frontend-design skill.

**Upload.tsx redesign:**
1. Page header: Match Sources/Users page header treatment from Plan 02 — display font title, description
2. Source selector dropdown: Style distinctively — custom dropdown appearance, accent border, icon treatment
3. State transitions: Add smooth transitions between the 4 states (SELECT_SOURCE → UPLOAD_FILE → MAP_COLUMNS → PROCESSING). Consider fade/slide transitions as state changes.
4. "New source" vs existing source selection: Make the choice visually clear with distinctive card-like options or a styled toggle/radio group
5. Overall page layout: Consider the spatial composition — the upload area is the hero element, source selector is secondary

**DropZone.tsx redesign:**
1. Default state: Create an atmospheric upload zone — NOT just a dashed border rectangle. Consider: subtle gradient background, grid pattern or dot texture, atmospheric glow, distinctive border treatment (gradient dashed border, animated dashes, or subtle pulse).
2. Icon: Make the upload icon more prominent and distinctive — larger, with accent color treatment, subtle float animation
3. "Drag & drop" text: Use display font, make it bold and clear
4. "Browse files" button: Style as a secondary action with distinctive treatment
5. Drag-over state: This is a key micro-interaction. When a file is dragged over:
   - Background should change dramatically (glow, color shift, scale)
   - Border should become solid and accent-colored with glow
   - Icon should animate (pulse, scale, or bounce)
   - Add a "Release to upload" text transition
6. File accepted feedback: Brief flash animation confirming the file was dropped
7. CSV-only hint text: Style subtly, not prominently

**CRITICAL: Preserve all functionality.** Drag-and-drop handling, file validation, onFileDrop callback, source selection, state machine transitions — all unchanged. Only visual presentation.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Upload page has styled source selector, smooth state transitions. DropZone has atmospheric default state with texture/gradient, dramatic drag-over interaction with glow and animation. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign ColumnMapper, ProgressTracker, ReUploadDialog, BatchHistory</name>
  <files>frontend/src/components/ColumnMapper.tsx, frontend/src/components/ProgressTracker.tsx, frontend/src/components/ReUploadDialog.tsx, frontend/src/components/BatchHistory.tsx</files>
  <action>
Read all 4 files. Apply the design system consistently.

**ColumnMapper.tsx redesign:**
1. Step indicator (1/2): Make the progress between steps visually rich — consider a progress bar with gradient fill, step circles with active/complete states, or a breadcrumb-style indicator
2. Step 1 (Name source): Form inputs match the Login/Sources input styling
3. Step 2 (Map columns): The canonical field → CSV header mapping rows need visual hierarchy:
   - Required fields (supplier_name, supplier_code) should be visually emphasized (accent border, badge, or icon)
   - Optional fields should be visually secondary
   - Dropdown selects: styled to match design system
   - Each mapping row should have subtle hover state
4. Navigation buttons (Back/Next/Save): styled with design system button treatment
5. Add transition animation between Step 1 and Step 2 (slide or fade)

**ProgressTracker.tsx redesign:**
1. This component visualizes a LIVE pipeline — it should feel ALIVE, not static
2. Pipeline stages: redesign as a horizontal or vertical pipeline with connecting lines/nodes
3. Completed stages: green/success treatment with checkmark animation (not just a static check icon)
4. Active stage: prominent with pulse-glow animation, spinner or animated icon, accent color glow
5. Pending stages: muted/grayed with dot or empty circle
6. Connecting lines between stages: animate as stages complete (line fills with color from left to right)
7. Stage labels: display font, clear hierarchy between stage name and status
8. Completion state: celebration treatment — all stages green, subtle glow effect, success message with animation
9. Failure state: danger color treatment with error icon and message

**ReUploadDialog.tsx redesign:**
1. Match the delete confirmation modal style from Sources page
2. Warning icon: prominent, amber/warning colored with glow
3. Impact counts text: make the numbers stand out (larger, accent colored, or bold)
4. Action buttons: "Cancel" secondary, "Continue" danger-styled with appropriate weight

**BatchHistory.tsx redesign:**
1. Match the Users page table design language for consistency
2. Status color coding: completed (success), failed (danger), processing (accent with pulse), superseded (muted)
3. Status badges: use the expressive badge style from Users page (glow effects)
4. Timestamps: style with mono font for alignment
5. Row counts: right-aligned, styled with number formatting
6. Add staggered row entrance animation
7. Empty state when no batches: styled message encouraging first upload

**CRITICAL: Preserve all functionality.** Column mapping logic, progress polling, re-upload confirmation flow, batch data display — all unchanged. Only visual presentation.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ColumnMapper has polished step indicator, styled mapping rows with required/optional hierarchy, transitions between steps. ProgressTracker feels alive with animated stage transitions, connecting lines, and celebration state. ReUploadDialog matches danger modal pattern. BatchHistory matches table design language. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero TypeScript errors
- All 6 files use design tokens from the updated app.css
- Upload flow preserves 4-state machine behavior (SELECT_SOURCE → UPLOAD_FILE → MAP_COLUMNS → PROCESSING)
- DropZone drag-and-drop functionality unchanged
- ColumnMapper wizard flow (step 1 → step 2 → save) unchanged
- ProgressTracker polling and stage display unchanged
- ReUploadDialog confirmation flow unchanged
- BatchHistory data display unchanged
</verification>

<success_criteria>
1. Upload experience feels cohesive and polished from start to finish — source selection through progress completion
2. DropZone is the most dramatic micro-interaction in the app — drag-over state is visually impressive
3. ProgressTracker feels alive — animated stage transitions communicate progress clearly
4. ColumnMapper wizard has clear visual progression between steps
5. ReUploadDialog and BatchHistory are consistent with the modal/table patterns from Plan 02
6. All upload functionality works exactly as before
7. TypeScript builds clean with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/1.1-design-polish/1.1-03-SUMMARY.md`
</output>
