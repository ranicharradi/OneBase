---
phase: 1.1-design-polish
plan: 02
type: execute
wave: 2
depends_on: ["1.1-01"]
files_modified:
  - frontend/src/pages/Sources.tsx
  - frontend/src/pages/Users.tsx
autonomous: true
requirements: [OPS-06]

must_haves:
  truths:
    - "Sources page uses the design system from Plan 01 — distinctive fonts, refined colors, animations"
    - "Users page uses the design system from Plan 01 — consistent visual language with Sources"
    - "Modals have atmospheric styling — not plain rounded rectangles on dark overlay"
    - "Tables/lists have visual rhythm — not identical-weight rows with no hierarchy"
    - "Loading skeletons use the shimmer animation from app.css, not plain pulse"
  artifacts:
    - path: "frontend/src/pages/Sources.tsx"
      provides: "Redesigned Sources CRUD page with distinctive design"
      min_lines: 400
    - path: "frontend/src/pages/Users.tsx"
      provides: "Redesigned Users management page with distinctive design"
      min_lines: 300
  key_links:
    - from: "frontend/src/app.css"
      to: "frontend/src/pages/Sources.tsx"
      via: "Design tokens and animation classes from Plan 01 design system"
      pattern: "surface-|accent-|animate-"
    - from: "frontend/src/app.css"
      to: "frontend/src/pages/Users.tsx"
      via: "Design tokens and animation classes from Plan 01 design system"
      pattern: "surface-|accent-|animate-"
---

<objective>
Apply the design system established in Plan 01 to the Sources and Users management pages. These are the primary data management surfaces — they need to feel like premium tools, not generic CRUD templates.

Purpose: Sources and Users pages are the most-used after Login. They contain modals, tables, forms, toasts, and loading states — all need the design system treatment.
Output: Redesigned Sources.tsx and Users.tsx with distinctive styling consistent with the design language from Plan 01.
</objective>

<execution_context>
@/home/rani/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rani/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.agents/skills/frontend-design/SKILL.md
@.planning/phases/1.1-design-polish/1.1-01-SUMMARY.md

Read the updated `frontend/src/app.css` to understand the design system established in Plan 01.
Read `frontend/src/components/Layout.tsx` to see the design language applied to the shell.

**Current state of files being modified:**
- `frontend/src/pages/Sources.tsx` — 512 lines. Full CRUD with: source list cards, create/edit modal with column mapping editor (required/optional canonical fields), delete confirmation modal, toast notifications, loading skeleton, empty state.
- `frontend/src/pages/Users.tsx` — 354 lines. User table with: avatar initials, active status badges, create user modal with password visibility toggle, loading skeleton, footer count.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Sources management page</name>
  <files>frontend/src/pages/Sources.tsx</files>
  <action>
Read the current `frontend/src/pages/Sources.tsx` (512 lines) and the Plan 01 SUMMARY + updated `frontend/src/app.css` to understand the design system.

Load the frontend-design skill at `.agents/skills/frontend-design/SKILL.md`. Apply the same aesthetic direction established in Plan 01.

**Page header:**
1. Make the "Sources" title use the display font with distinctive weight/size
2. Add a subtle description line styled with the body font
3. Style the "Add Source" button with presence — gradient, glow, icon, or distinctive shape. Not a plain colored rectangle.
4. Add entrance animation to the header elements

**Source list / cards:**
1. Give each source card visual depth — border treatment (gradient border, glow edge, or accent line), subtle background variation, atmospheric shadow
2. Add hover micro-interactions — translate, shadow change, border glow, or scale
3. Column mapping display within cards should have visual hierarchy — canonical field names styled differently from CSV header values
4. Action buttons (edit, delete) should have hover transitions
5. Add staggered entrance animation for cards (animation-delay per card index)

**Create/Edit modal:**
1. Backdrop: atmospheric blur with depth
2. Modal card: distinctive border treatment matching the design system. NOT plain rounded-2xl with white/6% border.
3. Form inputs: match the Login page input styling from Plan 01
4. Column mapping editor: the required/optional field sections should have clear visual hierarchy. Consider subtle section separators, icons, or color coding.
5. Dropdown selects: style to match the design system
6. Save/Cancel buttons: primary button matches the design system's button treatment, cancel is clearly secondary
7. Add entrance animation to the modal (scale + fade)

**Delete confirmation modal:**
1. Use danger color theming with atmospheric treatment
2. Warning icon should be more prominent
3. Button pair (cancel/delete) should have clear visual hierarchy

**Toast notifications:**
1. Style toasts with the design system — background blur, accent border, distinctive positioning
2. Add entrance/exit animations

**Loading skeleton:**
1. Replace plain pulse animation with the shimmer keyframe from app.css
2. Skeleton shapes should roughly match the actual content layout

**Empty state:**
1. Make the empty state visually interesting — icon, atmospheric treatment, inviting call-to-action
2. Not just gray text centered on screen

**CRITICAL: Preserve all functionality.** Do NOT change any state management, TanStack Query hooks, API calls, form validation, CRUD operations, or event handlers. Only change JSX structure, className strings, and visual presentation.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Sources page has distinctive design: atmospheric modals, cards with visual depth and hover interactions, staggered entrance animations, shimmer loading, styled toasts, compelling empty state. All CRUD functionality preserved. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Users management page</name>
  <files>frontend/src/pages/Users.tsx</files>
  <action>
Read the current `frontend/src/pages/Users.tsx` (354 lines) and apply the same design language used in Sources.

**Page header:**
1. Match the Sources page header treatment — display font title, description, styled action button
2. "Add User" button should match the "Add Source" button style for consistency

**User table:**
1. Table container: add visual depth — atmospheric background, subtle border treatment
2. Table header: distinctive styling — use the accent color subtly, uppercase labels with tracking, or a bottom border accent
3. Table rows: add hover state with smooth transition, alternating subtle background variation or uniform with dividers
4. Avatar initials: upgrade from plain gray circles — add gradient backgrounds based on username hash, ring/border treatment, or distinctive shape
5. Active status badges: make them more expressive — use glow effect for active, muted for inactive. Not just colored text.
6. Add staggered row entrance animation
7. Footer count: style as a subtle status bar

**Create user modal:**
1. Match the Sources modal design language exactly — same backdrop, card treatment, form styling
2. Password field with visibility toggle: style the toggle icon/button distinctively
3. Form validation error states should match the design system

**Loading skeleton:**
1. Table-shaped skeleton with shimmer animation
2. Match the actual table row layout

**CRITICAL: Preserve all functionality.** Only change visual presentation.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Users page has consistent design language with Sources: atmospheric table, gradient avatars, expressive status badges, styled modal, shimmer loading. All user management functionality preserved. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero TypeScript errors
- Sources.tsx uses design tokens from the updated app.css (new accent colors, animation classes)
- Users.tsx uses the same design language as Sources.tsx (consistent modals, buttons, animations)
- Both pages preserve all existing CRUD functionality
</verification>

<success_criteria>
1. Sources page feels like a premium data management tool — cards have depth, modals are atmospheric, interactions are animated
2. Users page is visually consistent with Sources — same design language applied to table/modal patterns
3. Loading states use shimmer animations, not plain pulse
4. Modals have distinctive border treatment and entrance animations
5. All CRUD functionality works exactly as before — create, read, update, delete sources and users
6. TypeScript builds clean with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/1.1-design-polish/1.1-02-SUMMARY.md`
</output>
