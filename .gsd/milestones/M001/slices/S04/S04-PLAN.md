# S04: Review Merge

**Goal:** Reviewers can examine pending match candidates, compare suppliers side-by-side with field-level conflict highlighting, and merge them field-by-field into golden records with full provenance.
**Demo:** User opens the review queue, sees match candidates sorted by confidence, opens a match detail view with side-by-side comparison and signal breakdowns, resolves field conflicts via radio buttons, and confirms a merge that produces a golden record with full provenance.

## Must-Haves


## Tasks


## Files Likely Touched

